
# Performance Optimization for Plumber APIs

This project contains apps written with the `{plumber}` and `{plumber2}` packages that utilize different strategies for increasing performance on metrics like throughput, resource utilization, etc.

# What's in the box

Below is a breakdown of the project.

```{r}
├── README.md
└── async (Show how async improves performance with I/O bound tasks)
```
